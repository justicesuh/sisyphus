services:
  django:
    build: .
    container_name: sisyphus_django
    command: /bin/bash
    ports:
      - 8000:8000
    restart: unless-stopped
    stdin_open: true
    tty: true
    volumes:
      - ./:/usr/app
    working_dir: /usr/app
